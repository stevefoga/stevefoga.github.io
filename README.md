# personal-website

## Local server testing
1. Install `npm`: `brew install nodejs`
2. Install `http-server`: `npm install -g http-server`
3. Start server: `http-server`
4. Browse to http://localhost:8080